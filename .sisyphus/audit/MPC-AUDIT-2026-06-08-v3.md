# MPC Security Audit — PVTHFHE (2026-06-08 v3 — Fresh Deep Dive with Regressions Re-Verified)

**Auditor**: Sisyphus with `mpc-audit` skill
**Scope**: Full repo — Rust crates (23), Noir circuits (7), Solidity contracts (10), design docs
**Model**: Actively malicious adversary, honest-majority threshold t = ⌊n/2⌋ + 1, static PPT, synchronous network
**Trust Model**: Only the verifier at any given phase is trusted. Native code is untrusted.
**Baseline**: Git HEAD, code-level verification of all prior finding claims
**Remediation**: Implemented in `.sisyphus/plans/mpc-audit-2026-06-08-v3-remediation.md`

---

## Executive Summary

**Significant progress since v2 audit.** Of the 8 prior regressions, **6 are now FIXED** in code. Two remain partially addressed (F0 fold challenge at 64-bit vs recommended 128-bit; FF10 LaZer binding computed but not injected). Three fresh LOW/MEDIUM findings identified and remediated.

**Overall assessment**: The codebase's structural hygiene is excellent — 71 domain-separation tags, centralized wire format validation, typed error enums with identifiable aborts, real CCS satisfiability checks, and committed-smudge enforcement. The critical architectural risk remains documented open problems P1 (lattice NIZK soundness), P2 (LatticeFold+ Lemma 9), P4 (on-chain IVC decider), and G-N8 (N=8 circuit vs N=8192 production).

---

## Prior Finding Verification (Code-Level)

### F0: Fold Challenge Bit-Width — PARTIALLY FIXED (IMPROVED, u64→u128 planned)
`fold.rs:62`: `u64::from_le_bytes(h[..8]...)` — 64-bit from 256-bit hash. Prior was 16-bit.

### F1: `ajtai_sigma_session_binding` Domain Separator — ✅ FIXED
`adapter.rs:665-675`: Tag::CycloAjtaiBinding.as_bytes() + length prefixes.

### F2: Witness Length Validation — ✅ FIXED
`adapter.rs:431-439`: Exact rlwe_n() length checks for both fields.

### FF5: Schnorr PoP — ✅ FIXED (unchanged)
### FF10: LaZer Bridge Session/Party Binding — PARTIALLY FIXED
Binding computed but not injected into LaZer C library.
### C6: Committed-Smudge Enforcement — ✅ FIXED
### N1: Fold Challenge Missing params_digest — ✅ FIXED
### N2: CCS Witness Soundness in verify_fold — ✅ FIXED
### N3: challenge_bytes Counter-Mode Label — ✅ FIXED
### N4: Poseidon Panics — ✅ FIXED
### N5: encode_u64s_le/i64s_le Overflow — ✅ FIXED

---

## Fresh Findings (This Audit)

### FN1: derive_challenge Returns u64 Instead of u128 — REMEDIATED
`fold.rs:46-62` extracted 64 bits. Changed to u128 (16 bytes). See remediation plan.
**Impact**: Low (exponential bound dominance). **Severity**: MEDIUM.

### FN2: Sigma Challenge Fallback Returns 0 Silently — REMEDIATED
`sigma.rs:694-697` silently returned 0 on Poseidon failure. Changed to `?` error propagation.
**Impact**: Wasted round of soundness. **Severity**: LOW.

### FN3: Secret::into_inner() Safety Gap — LOW
Documentation-only risk. Not reachable in production paths.

---

## Trust Boundary Summary

| Component | Trusted? | Key Gaps |
|-----------|----------|------|
| Native sigma verifier | ❌ | P1 (conditional), FN2 (fixed) |
| Cyclo accumulator | ❌ | FN1 (fixed to 128-bit), P2 (Lemma 9) |
| LatticeFold+ compressor | ❌ | G-N8 (N=8 → N=8192 projection) |
| Noir aggregator_final | ✅ (on-chain anchor) | G-N8 (N=8 prototype) |
| PvtFheVerifier.sol | ✅ (on-chain) | P4 (IVC decider fail-closed) |

---

## Remediation

See `.sisyphus/plans/mpc-audit-2026-06-08-v3-remediation.md` for full details.

| Priority | Finding | Fix | Status |
|----------|---------|-----|--------|
| P0 | FN1 (u64 → u128) | Changed derive_challenge return type, scalar_mul, public_io_v1, tests | ✅ IMPLEMENTED |
| P1 | FF10 (LaZer binding) | Integration tests for session/party binding | Deferred |
| P2 | FN2 (silent fallback) | `return 0` → `?` with Result return type | ✅ IMPLEMENTED |

---

*Audit version*: 7.0 (code-level verification — 6 of 8 prior regressions fixed, 3 new findings, 2 remediated)
