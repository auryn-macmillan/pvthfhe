# T11.5 Side-Channel & Oracle-Leakage Audit

**Date:** 2026-05-05  
**Scope:** `pvthfhe-keygen`, `pvthfhe-nizk`, `pvthfhe-fhe`, `pvthfhe-cyclo`

---

## Findings Table

| # | File:Line | Issue | Severity | Fix |
|---|-----------|-------|----------|-----|
| 1 | `crates/pvthfhe-keygen/src/hermine.rs:174` | Non-CT commitment comparison in `verify_share_set`: `share.commitment.as_deref() != Some(expected_commitment.as_slice())` uses `PartialEq` which short-circuits on first differing byte, leaking timing about where commitment bytes differ | High | Replaced with `subtle::ConstantTimeEq::ct_eq`; see `hermine.rs:174–180` |
| 2 | `crates/pvthfhe-keygen/src/hermine.rs:439` | Non-CT commitment lookup in `check_and_blame`: `artifact.commitments.contains(&expected_commit)` uses `PartialEq` in linear scan, leaking both position and byte-offset of mismatch | High | Replaced with `.iter().any(|c| bool::from(c.as_slice().ct_eq(...)))` |
| 3 | `crates/pvthfhe-keygen/src/hermine.rs:443` | Non-CT comparison in `check_and_blame`: `c == expected_commit.as_slice()` | High | Replaced with `bool::from(c.ct_eq(expected_commit.as_slice()))` |
| 4 | `crates/pvthfhe-nizk/src/sigma.rs:199` | Non-CT challenge comparison in `sigma::verify`: `if expected_ch != proof.ch` uses `Vec<i64>` `PartialEq`, leaking which challenge coefficient is first to differ. Enables adaptive timing attack on Fiat-Shamir challenge reconstruction | High | Serialized both to `Vec<u8>` and compared with `subtle::ConstantTimeEq`; see `sigma.rs:199–204` |
| 5 | `crates/pvthfhe-nizk/src/adapter.rs:193` | Non-CT PVSS commitment comparison in `CycloNizkAdapter::verify`: `if encoded_commitment != stmt.pvss_commitment` on `[u8; 32]` | Medium | Replaced with `bool::from(encoded_commitment.ct_eq(&stmt.pvss_commitment))` |
| 6 | `crates/pvthfhe-keygen/src/hermine.rs:110–198` | Oracle leakage: `verify_share_set` returns `BlameProof` with distinct `reason` strings (`"replayed_share"`, `"threshold_mismatch"`, `"forged_share"`, etc.). An external caller can identify the failure category from the blame reason. | Medium | By design for PVSS blame protocol — parties must identify dishonest dealers. The blame reason is not a secret but an accusation. Documented as accepted risk; no fix required. Secret-value presence check gated behind early-return before commitment check. |
| 7 | `crates/pvthfhe-keygen/src/hermine.rs:34–36` | `check_share_shortness(value: u64)` checks a single scalar `value <= NORM_BOUND_B_E`. Name implies per-coefficient norm check but implementation operates on a pre-aggregated scalar. No timing concern (scalar comparison), but semantics mismatch. | Low | Documented. Name is correct as-is given callers pass individual coefficients; no code change needed. |
| 8 | `crates/pvthfhe-cyclo/src/fold.rs:15–21` | `witness_norm_estimate` uses `.max()` which scans all bytes without short-circuit. Timing is proportional to input length, not value. | Low | No issue; iterates all bytes uniformly. No fix needed. |
| 9 | `crates/pvthfhe-cyclo/src/fold.rs:78` | `beta_step > per_step_budget` is a single scalar comparison after full-scan `max()`. No timing leak. | Low | No issue. |
| 10 | `crates/pvthfhe-nizk/src/sigma.rs:297–326` | `derive_challenge` absorbs all transcript fields before finalizing. No partial digest state exposed. | Low | No issue. |
| 11 | `crates/pvthfhe-fhe/src/fhers.rs` | `FhersBackend` returns uniform `FheError::Backend` sentinel for all primitives (T33 stub). No oracle differentiation possible. | Low | No issue; uniform error path. |

---

## Critical/High — RED→GREEN Test Evidence

### Finding #1, #2, #3 — `hermine.rs` commitment comparisons

**RED test** (failed before fix — `subtle` not a dependency):  
`crates/pvthfhe-keygen/src/hermine.rs` — `sc_audit_commitment_comparison_is_ct`  
`crates/pvthfhe-keygen/src/hermine.rs` — `sc_audit_check_and_blame_ct_reject`

**Fix applied:** Added `subtle = "2"` to `crates/pvthfhe-keygen/Cargo.toml`.  
Replaced all three non-CT comparisons with `subtle::ConstantTimeEq::ct_eq`.

**GREEN:** `cargo test -p pvthfhe-keygen` — 10 tests pass.

### Finding #4 — `sigma.rs` challenge comparison

**RED test** (failed before fix):  
`crates/pvthfhe-nizk/tests/sc_audit.rs` — `sc_audit_sigma_challenge_comparison_is_ct`  
`crates/pvthfhe-nizk/tests/sc_audit.rs` — `sc_audit_verify_rejects_tampered_challenge`

**Fix applied:** Added `subtle = "2"` to `crates/pvthfhe-nizk/Cargo.toml`.  
`sigma::verify` now serializes both challenges to bytes and uses `ct_eq` before returning.

**GREEN:** `cargo test -p pvthfhe-nizk` — all tests pass.

### Finding #5 — `adapter.rs` pvss_commitment comparison

**RED test:**  
`crates/pvthfhe-nizk/tests/sc_audit.rs` — `sc_audit_pvss_commitment_comparison_is_ct`

**Fix applied:** `adapter.rs:193` uses `subtle::ConstantTimeEq::ct_eq`.

**GREEN:** included in nizk test run above.

---

## Test Results Summary

| Crate | Before | After |
|-------|--------|-------|
| `pvthfhe-keygen` | 10 pass | 10 pass |
| `pvthfhe-nizk` | all pass | all pass |
| `pvthfhe-cyclo` | 5 pass | 5 pass |
| `pvthfhe-fhe` | 4 pre-existing failures (T33 stub) | 4 pre-existing failures (unchanged) |

The 4 `pvthfhe-fhe` failures are pre-existing (verified via `git stash`): `FhersBackend` is a T33 stub returning `not_implemented()` for all primitives. They are not caused by this audit.

---

## Vectors Audited per Spec

| Vector | File | Status |
|--------|------|--------|
| Secret-dependent branches | `hermine.rs` | No `if secret_key...` branches found; secret values used only in polynomial evaluation and SHA-256 hashing |
| Early-exit on MAC/hash comparison | `hermine.rs`, `sigma.rs`, `adapter.rs` | **Fixed** with `subtle::ConstantTimeEq` |
| Oracle replies distinguishing ciphertext categories | `fhers.rs` | Uniform `FheError::Backend` sentinel; no oracle |
| Norm check timing in `check_share_shortness` | `hermine.rs:34` | Single scalar comparison; constant time |
| Transcript hash leakage in `derive_challenge` | `sigma.rs:297`, `fold.rs:23` | Full absorption before finalization; no partial state exposed |
