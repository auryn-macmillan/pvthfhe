- 2026-05-07: Helpers originally local to pvthfhe_e2e.rs and now shared/moved: Compressor enum, compressor_inputs, compressor_error_to_anyhow, assert_surrogate_compressor_acknowledged, compressor_backend_id, log_compressor_mode, plus E2eCompressedProof in crates/pvthfhe-cli/src/compressor_glue.rs.
- 2026-05-07: Shared pipeline in crates/pvthfhe-cli/src/full_pipeline.rs continues using existing shared build_demo_nizk_inputs from crates/pvthfhe-cli/src/demo_nizk.rs; main.rs helpers were only read, not modified in this slice.
- 2026-05-07: Bench observer populates E2eTimings fields keygen.total_ms/instances_run, nizk_prove.{total_ms,instances_run,per_instance_ms}, nizk_verify.{total_ms,instances_run,per_instance_ms}, pvss_share_encrypt.{total_ms,instances_run,deal_ms,verify_ms,recover_ms}, pvss_decrypt_prove.{total_ms,instances_run,per_instance_ms}, cyclo_fold.{total_ms,instances_run}, compressor_prove.{total_ms,instances_run}, compressor_verify.{total_ms,instances_run}, partial_decrypt.{total_ms,instances_run,per_instance_ms}, aggregate_decrypt.{total_ms,instances_run}, noir_sonobe_wrap.{total_ms,instances_run}, onchain_verify.{total_ms,instances_run}.
- 2026-05-07: Gotcha: Compressor::prove signature remains prove(&CycloFoldAllReport) -> anyhow::Result<E2eCompressedProof>; sonobe mode hashes inner.compressed_proof_bytes(&proof) for digest while verify recomputes digest from the same bytes.
- 2026-05-07: Demo clap help currently prints defaults as `[default: 4]` for `--n` and `[default: 0]` for `--seed`; that makes `demo_defaults.rs` a clean RED guard for the D2 defaults update.
- 2026-05-07: `demo_runs_full_pipeline.rs` uses `Command::cargo_bin("pvthfhe-cli")` and case-insensitive combined stdout/stderr matching; current demo output still stops at `partial_decrypt`/`aggregate_decrypt` and lacks `pvss_share_encrypt`, `cyclo_fold`, and compressor markers.
- 2026-05-07: `main.rs::run_demo` now acts as a thin wrapper over `full_pipeline::run_full_pipeline` via a local `DemoObserver`; summary lines come from `PipelineReport.timings`, with `aggregate_keygen_ms` and `encrypt_ms` captured from observer `phase_end` hooks because those fields are not persisted in `E2eTimings`.
- 2026-05-07: Banner decision D3 applied in demo output/tests: kept `backend_id == \"cyclo-ajtai-d2-conditional\"`, `backend_id_p2`, `backend_id_p3`, and `pvss_backend_id`, added the explicit note that on-chain Solidity verification is not run by `demo`, and dropped the old `P1 NIZK: conditional soundness only` banner line because repo docs discuss the limitation but do not require that exact banner copy.
## [2026-05-07] Step 9: Justfile demo-e2e recipe updated to n=8 t=5 --release --features sonobe-compressor

## [2026-05-07] Step 10: README Quickstart updated to reflect n=8 t=5 full-pipeline demo
