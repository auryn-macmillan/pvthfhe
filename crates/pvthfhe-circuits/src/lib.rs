//! # ⚠️ INTENTIONALLY MINIMAL
//!
//! Façade crate for Noir circuit Rust bindings. Real circuits live in `circuits/` (Noir workspace). This Rust crate exists for workspace `Cargo.toml` integration only; expanding it would duplicate Noir-side artifacts.
#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {}
}
