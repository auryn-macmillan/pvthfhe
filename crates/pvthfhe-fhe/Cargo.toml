[package]
name = "pvthfhe-fhe"
version = "0.1.0"
edition = "2021"

[features]
mock = []

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
rand_core = "0.6"

[dev-dependencies]
rand = "0.8"
